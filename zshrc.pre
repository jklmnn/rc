
#check for terminal emulator
if [ $(command -v tmux) ]
then
    term=$(ps -h -o comm -p $(ps -h -o ppid -p $$))
    case $term in
	(login) tmux -u; exit;
	    ;;
	(sshd) tmux -u; exit;
	    ;;
    esac
fi

